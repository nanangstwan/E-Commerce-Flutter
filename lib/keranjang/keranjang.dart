import 'package:flutter/material.dart';

class MyKeranjang extends StatelessWidget {
  const MyKeranjang({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Keranjang'),
      ),
    );
  }
}
