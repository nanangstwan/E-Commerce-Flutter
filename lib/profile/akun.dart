import 'package:flutter/material.dart';

class MyAkun extends StatelessWidget {
  const MyAkun({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Profile Akun'),
      ),
    );
  }
}